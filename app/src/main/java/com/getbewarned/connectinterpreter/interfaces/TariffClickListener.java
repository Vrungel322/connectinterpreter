package com.getbewarned.connectinterpreter.interfaces;

import com.getbewarned.connectinterpreter.models.TariffItem;

public interface TariffClickListener {
    void onTariffClick(TariffItem item);
}
